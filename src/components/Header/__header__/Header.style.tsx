import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  helloParagraph: {
    color: "white",
    fontSize: 12,
    backgroundColor: "blue",
    background: "blue",
  },
}));
