import { makeStyles } from "@mui/styles";

export const useStyles: any = makeStyles(() => ({
  filterBar: {
    borderTop: "1px solid grey",
    paddingTop: 5,
  },
  filterDisplay: {
    marginLeft: 20,
  },
}));
